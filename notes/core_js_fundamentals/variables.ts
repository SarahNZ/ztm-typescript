let message = "hello";
message = "goodbye";

const message2 = "hello";
message2 = "goodbye";
// const values are mutable (can be changed), but we can't asign new data ot it

// Don't use legacy var keyword. I.e. var message = "hello";